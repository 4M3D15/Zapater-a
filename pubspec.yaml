name: zapato
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.4  # Solo aquí

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/cortez.png
    - assets/YZ1.png
    - assets/55810NB.png
    - assets/YZPINK.png
    - assets/courtvision.png
    - assets/55412NB.png
